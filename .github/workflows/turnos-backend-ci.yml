name: Turnos Profesionales - Backend CI

# CI del backend de TURNOS-2026-001 (proyectos/turnos-profesionales/05-codigo/backend).
# Fase 6 (DevOps) - ver proyectos/turnos-profesionales/08-despliegue/README.md para el
# detalle completo de lo que valida este workflow y por que esta dividido en dos jobs.
#
# Nota (2026-08-06): 073dc23 no disparo ningun run pese a tocar este archivo (dentro del
# filtro de paths de abajo) - la caida de Actions de ese dia afecto tambien la entrega de
# eventos push, no solo la ejecucion de jobs ya encolados. Este comentario es un cambio real
# (no un commit vacio, que el filtro de paths hubiera ignorado de cualquier forma) para
# volver a intentar el trigger una vez que GitHub confirmo el servicio restablecido.
#
# Nota (2026-08-07): ambos jobs corren ahora contra PostgreSQL real (service container),
# coordinado con Backend migrando el driver de acceso a datos de `node:sqlite` a `pg` en
# paralelo a este cambio - no son dos cambios independientes, el primer push que junte ambos
# es el que da la primera senal real de si la migracion funciona de punta a punta. Ver el
# bloque "services" + "env: DATABASE_URL" de cada job para las credenciales de test (fijas,
# no secretas - contenedor efimero del run) y proyectos/turnos-profesionales/08-despliegue/
# README.md para el detalle completo (incluida la misma DATABASE_URL para docker-compose.yml
# y para un despliegue real en Render).
#
# Nota (2026-08-09/10, DevOps) - separacion de roles de Postgres (cierre de gap CRITICAL de
# TURNOS-2026-001). POSTGRES_USER=turnos_ci (ver "services" de cada job) es SUPERUSUARIO DEL
# CLUSTER - asi lo crea la imagen oficial postgres:16-alpine via esa variable - y un
# superusuario bypassea Row Level Security siempre, FORCE ROW LEVEL SECURITY incluido (ver
# proyectos/turnos-profesionales/05-codigo/database/migrations/001_init.sql, bloque
# "Separacion owner/runtime y FORCE ROW LEVEL SECURITY"). Ambos jobs agregan ahora, apenas
# despues de "Checkout", los pasos "Roles Postgres 0/4"-"4/4": aseguran que psql este
# disponible en el runner, provisionan un rol de MIGRACION (owner, corre
# database/migrations/001_init.sql una sola vez) y un rol de RUNTIME (sin privilegios de owner,
# sin BYPASSRLS - el que usa DATABASE_URL/`docker run -e DATABASE_URL` de ahi en adelante) via
# database/scripts/provisionar_roles_postgres.sql (DBA), y por ultimo corren
# database/scripts/verificar_rls_postgres.sql (Security) para confirmar contra el service
# container real que Row Level Security filtra de verdad - no solo que el DDL compila. Mismo
# patron que docker-compose.yml (ver ese archivo y
# proyectos/turnos-profesionales/05-codigo/backend/docker/postgres-initdb/*.sh), adaptado a que
# estos pasos corren en el runner por TCP a localhost, no dentro del contenedor de Postgres
# (un service container de Actions no tiene el repo disponible todavia cuando arranca, asi que
# el mecanismo docker-entrypoint-initdb.d que usa docker-compose.yml no aplica aca).

on:
  push:
    paths:
      - "proyectos/turnos-profesionales/05-codigo/backend/**"
      - ".github/workflows/turnos-backend-ci.yml"
  pull_request:
    paths:
      - "proyectos/turnos-profesionales/05-codigo/backend/**"
      - ".github/workflows/turnos-backend-ci.yml"

defaults:
  run:
    working-directory: proyectos/turnos-profesionales/05-codigo/backend

env:
  # Version de Node fijada a proposito, coordinada con el Dockerfile (node:22-slim) - no
  # bajar de 22 aca sin revisar los dos archivos juntos. Historicamente este piso salia de
  # que src/db.ts usaba el modulo built-in `node:sqlite` (exige Node >=22.5) - con Backend
  # migrando el driver a `pg` (ver "services"/"DATABASE_URL" mas abajo), `pg` en si no exige
  # ningun piso de Node tan alto, pero se mantiene Node 22 igual por consistencia con el
  # Dockerfile de produccion, no porque el driver nuevo lo requiera.
  NODE_VERSION: "22"

jobs:
  # ---------------------------------------------------------------------------------------
  # Job 1: valida el CODIGO FUENTE - tipado estricto + la bateria completa de scripts de
  # prueba (business logic + regresiones de seguridad ya remediadas, ver
  # 07-seguridad/informe-seguridad.md). Corre contra ts-node (igual que `npm run dev`,
  # sin pasar por dist/) para dar feedback rapido sin depender del layout del build.
  # ---------------------------------------------------------------------------------------
  build-and-test:
    name: Build + type-check + bateria de scripts
    runs-on: ubuntu-latest
    # `contents: write` habilita el ultimo paso (comentario en el commit con los logs de una
    # falla) - mismo motivo/patron que turnos-mobile-ci.yml (ver ese workflow, commit cb20d12).
    permissions:
      contents: write
    # Service container de PostgreSQL real (2026-08-07). Actions lo arranca ANTES que
    # cualquier step de este job y espera a que su healthcheck (--health-cmd de "options" de
    # abajo - no confundir con el HEALTHCHECK del Dockerfile del backend, son cosas
    # distintas) este "healthy" antes de dejar correr el primer step: no hace falta un loop
    # propio de espera como el que si usan mas abajo los pasos "arrancar servidor" para
    # /health del backend. Credenciales fijas y NO secretas a proposito: este Postgres vive
    # solo durante este run, en la red interna del job, nunca se expone fuera de el - mismo
    # criterio que el JWT_SECRET fijo que ya usa el job docker-build-smoke de mas abajo.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: turnos_ci
          POSTGRES_PASSWORD: turnos_ci_password
          POSTGRES_DB: turnos_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U turnos_ci -d turnos_test"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    # DATABASE_URL a nivel de JOB: la heredan todos los steps de abajo, incluidos los que ya
    # definen su propio "env:" (PORT/EXPIRACION_PAGO_MIN/etc. en los pasos "arrancar
    # servidor") - el env de un step se COMBINA con el del job, no lo reemplaza entero. Host
    # "localhost" porque los steps de este job corren directo en la maquina del runner (no
    # dentro de otro contenedor) y así es como Actions publica el puerto de un service
    # container en ese caso - ver docker-compose.yml para el equivalente en desarrollo local,
    # donde el host es el nombre del servicio ("postgres"), no "localhost".
    #
    # OJO - esto cambia un supuesto que tenian los dos arranques del servidor de mas abajo:
    # con node:sqlite, "Fase 1/2 - detener servidor y limpiar la DB" borraba el archivo
    # dev.sqlite3 entre Fase 1 y Fase 2, asi que Fase 2 arrancaba siempre con una base VACIA.
    # Este Postgres es el MISMO contenedor para ambas fases (no hay archivo que borrar), asi
    # que Fase 2 arranca con los datos que dejo Fase 1 todavia presentes. Es seguro porque
    # scripts/*.mjs ya esta escrito para convivir con datos de otros scripts en la misma
    # corrida (emails/nombres unicos por timestamp - ver p.ej. el comentario de
    # scripts/test-rn8-ventana-cancelacion.mjs: "No depende de otros scripts"), pero no se
    # audito exhaustivamente cada uno de los 10 scripts contra este supuesto especifico: si
    # alguno asume implicitamente una base vacia, va a fallar recien aca.
    # DATABASE_URL apunta al rol de RUNTIME (turnos_ci_app), NUNCA al superusuario de bootstrap
    # (turnos_ci, ver "services" arriba) ni al rol de MIGRACION - separacion de roles
    # 2026-08-09/10, ver la nota grande del encabezado de este archivo. Ese rol todavia no
    # existe en este punto del archivo (lo crea el paso "Roles Postgres 1/4" mas abajo, ANTES
    # de que ningun otro paso use esta variable) - resolver DATABASE_URL con este valor desde
    # ahora es seguro porque ningun step anterior a los pasos "Roles Postgres */4" toca la base
    # de datos.
    env:
      DATABASE_URL: postgresql://turnos_ci_app:turnos_ci_app_password@localhost:5432/turnos_test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: "Roles Postgres 0/4 - psql en el runner (ubuntu-latest ya lo trae; instalar si faltara)"
        run: |
          if ! command -v psql > /dev/null 2>&1; then
            sudo apt-get update && sudo apt-get install --yes postgresql-client
          fi
          psql --version

      - name: "Roles Postgres 1/4 - provisionar rol de MIGRACION y rol de RUNTIME"
        env:
          PGPASSWORD: turnos_ci_password
        run: |
          set +e
          {
            # Corre TODO database/scripts/provisionar_roles_postgres.sql (Parte 1 + Parte 2, sin
            # modificarlo) conectado como el superusuario de bootstrap (turnos_ci). A PROPOSITO
            # SIN "-v ON_ERROR_STOP=1": corriendo ANTES de la migracion, ninguna tabla/funcion
            # existe todavia, asi que los 2 GRANT EXECUTE ON FUNCTION finales de la Parte 2 van a
            # fallar aca A PROPOSITO ("function ... does not exist" - se completan en el paso
            # 3/4). Sin ON_ERROR_STOP, psql no aborta por eso (imprime el error y sigue) - mismo
            # comportamiento que anticipa el propio comentario de ese script sobre por que es
            # seguro ignorar el error puntual de "CREATE ROLE ... already exists" al re-correrlo.
            #
            # OJO con las contrasenas: se pasan CRUDAS (sin comillas extra), a diferencia del
            # ejemplo del header de ese script (-v migrador_password="'CAMBIAR_1'") - el SQL ya
            # usa :'migrador_password' (sustitucion YA entre comillas); duplicar la cita
            # guardaria una contrasena con comillas literales de mas. Mismo criterio que
            # proyectos/turnos-profesionales/05-codigo/backend/docker/postgres-initdb/01-provisionar-roles.sh.
            psql --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -v migrador_rol=turnos_ci_migrador -v migrador_password=turnos_ci_migrador_password \
              -v app_rol=turnos_ci_app -v app_password=turnos_ci_app_password \
              -v nombre_db=turnos_test \
              -f ../database/scripts/provisionar_roles_postgres.sql

            # GRANT adicional, no incluido en provisionar_roles_postgres.sql a proposito (ese
            # script deja expresamente afuera de su alcance el CREATE sobre el esquema para el
            # rol de RUNTIME). El rol de MIGRACION si necesita CREATE sobre "public" para poder
            # correr 001_init.sql (CREATE EXTENSION/TYPE/TABLE) en el paso 2/4 - desde Postgres
            # 15, "public" ya no le otorga CREATE a PUBLIC por defecto, y no se hereda a un rol
            # nuevo sin este GRANT explicito.
            psql -v ON_ERROR_STOP=1 --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -c 'GRANT CREATE ON SCHEMA public TO turnos_ci_migrador;'
          } > roles-paso1.log 2>&1
          code=$?
          cat roles-paso1.log
          exit $code

      - name: "Roles Postgres 2/4 - migrar el esquema como el rol de MIGRACION"
        env:
          PGPASSWORD: turnos_ci_migrador_password
        run: |
          set +e
          {
            # Conectado COMO el rol de MIGRACION (no turnos_ci) - asi ese rol, no el
            # superusuario de bootstrap, queda como OWNER de las tablas/tipos/funciones. Con
            # ON_ERROR_STOP=1: sobre una base recien provisionada esto no tiene ningun motivo
            # esperable para fallar.
            psql -v ON_ERROR_STOP=1 --host localhost --port 5432 --username turnos_ci_migrador --dbname turnos_test \
              -f ../database/migrations/001_init.sql
          } > roles-paso2.log 2>&1
          code=$?
          cat roles-paso2.log
          exit $code

      - name: "Roles Postgres 3/4 - completar GRANTs (funciones SECURITY DEFINER, ya existen)"
        env:
          PGPASSWORD: turnos_ci_password
        run: |
          set +e
          {
            # Re-correr provisionar_roles_postgres.sql completo - hace falta repetirlo para que
            # corran de verdad los 2 GRANT EXECUTE ON FUNCTION que fallaron a proposito en el
            # paso 1/4 (las funciones no existian todavia). El resto es idempotente al repetirse
            # (y el GRANT sobre "ALL TABLES" ahora si alcanza a las tablas reales), salvo los 2
            # CREATE ROLE de la Parte 1, que aca SI van a fallar con "already exists" - tolerado
            # a proposito, otra vez sin "-v ON_ERROR_STOP=1", mismo criterio que el paso 1/4.
            psql --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -v migrador_rol=turnos_ci_migrador -v migrador_password=turnos_ci_migrador_password \
              -v app_rol=turnos_ci_app -v app_password=turnos_ci_app_password \
              -v nombre_db=turnos_test \
              -f ../database/scripts/provisionar_roles_postgres.sql
          } > roles-paso3.log 2>&1
          code=$?
          cat roles-paso3.log
          exit $code

      - name: "Roles Postgres 4/4 - verificar RLS a nivel de base de datos (verificar_rls_postgres.sql, Security)"
        env:
          PGPASSWORD: turnos_ci_password
        run: |
          set +e
          {
            # Conectado como el superusuario de bootstrap (turnos_ci), NO como el rol de
            # RUNTIME: el propio script necesita hacer `SET ROLE` a AMBOS roles de prueba
            # (migrador y runtime) dentro de la misma sesion/transaccion (ver su encabezado,
            # "CÓMO CORRERLO") - un superusuario puede siempre SET ROLE a cualquier rol sin
            # necesitar membresia explicita. Deliberadamente NO se le otorga al rol de RUNTIME
            # membresia en el rol de MIGRACION solo para poder conectarse "como runtime" aca: en
            # Postgres esa membresia (heredable, el default) haria que el rol de RUNTIME
            # herede tambien la exencion de RLS del owner, exactamente el bypass que toda esta
            # separacion de roles busca evitar - un regression de seguridad peor que la
            # comodidad de esta unica conexion de diagnostico. Las aserciones que importan
            # (Partes 2 y 4 del script) igual ejercitan el comportamiento real del rol de
            # RUNTIME: el script hace `SET ROLE turnos_ci_app` internamente antes de correrlas.
            psql -v ON_ERROR_STOP=1 --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -v migrador_rol=turnos_ci_migrador -v app_rol=turnos_ci_app \
              -f ../database/scripts/verificar_rls_postgres.sql
          } > roles-verificar-rls.log 2>&1
          code=$?
          cat roles-verificar-rls.log
          exit $code

      - name: Setup Node ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"
          cache-dependency-path: proyectos/turnos-profesionales/05-codigo/backend/package-lock.json

      - name: Instalar dependencias (npm ci)
        run: npm ci

      - name: Type-check estricto (tsc --noEmit)
        run: npx tsc --noEmit

      - name: Compilar (npm run build) - detecta errores de build ademas de tipos
        run: npm run build

      # ── Bateria de scripts/*.mjs (~10 scripts) ──────────────────────────────────────
      # OJO: no alcanza con UN solo arranque del servidor. scripts/test-rn8-ventana-
      # cancelacion.mjs esta disenado a proposito contra la ventana de cancelacion
      # DEFAULT (120 min, ver src/routes/turnos.ts) y FALLA si se lo corre con
      # VENTANA_CANCELACION_MIN=0 (que es justamente lo que necesitan los otros 9
      # scripts para no depender de esperar minutos reales) - confirmado en este ciclo
      # reproduciendo el fallo real antes de escribir este workflow, no es algo teorico.
      # Por eso este job levanta el servidor DOS veces, tal como documenta el propio
      # README.md del backend ("Scripts de prueba (actualizado)").

      - name: "Fase 1/2 - arrancar servidor (ventanas en 0, rate limit alto, dev routes on)"
        env:
          PORT: "3000"
          EXPIRACION_PAGO_MIN: "0"
          VENTANA_CANCELACION_MIN: "0"
          ENABLE_DEV_ROUTES: "true"
          RATE_LIMIT_LOGIN_MAX: "1000"
          RATE_LIMIT_REGISTRO_MAX: "1000"
        run: |
          # Invocar el binario de node_modules/.bin DIRECTO, no "npx ts-node" (ver el comentario
          # largo en el paso "detener servidor" de mas abajo para la causa raiz completa) - esto
          # es lo que hace que "$!" mas abajo capture el PID real del servidor.
          node_modules/.bin/ts-node --transpile-only src/index.ts > server-fase1.log 2>&1 &
          echo $! > server.pid
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3000/health > /dev/null; then
              echo "Servidor (fase 1) arriba."
              exit 0
            fi
            sleep 1
          done
          echo "El servidor (fase 1) no respondio /health a tiempo."
          cat server-fase1.log
          exit 1

      - name: "Fase 1/2 - correr todos los scripts salvo test-rn8-ventana-cancelacion"
        run: |
          set +e
          {
            for f in scripts/*.mjs; do
              case "$f" in
                scripts/test-rn8-ventana-cancelacion.mjs) continue ;;
              esac
              echo "== Corriendo $f =="
              node "$f" || exit 1
            done
          } > scripts-fase1.log 2>&1
          code=$?
          cat scripts-fase1.log
          exit $code

      - name: "Fase 1/2 - logs del servidor (si algo fallo)"
        if: failure()
        run: cat server-fase1.log || true

      - name: "Fase 1/2 - detener servidor y limpiar la DB"
        if: always()
        run: |
          # CAUSA RAIZ CONFIRMADA del fallo real de este job (run 31108620102 - reproducido en
          # local antes de este fix, ver memory/proyectos/turnos-profesionales/decisiones.md):
          # el paso anterior arrancaba el servidor con "npx ts-node ... &" y guardaba "$!" como
          # el PID a matar acá. Pero "npx" SIEMPRE hace spawn de un proceso hijo real para correr
          # un binario local (nunca reemplaza su propio proceso vía exec), así que "$!" capturaba
          # el PID del WRAPPER de npx, no el del proceso de Node que realmente terminaba
          # bindeando el puerto 3000 — matar solo ese wrapper dejaba el servidor real HUÉRFANO Y
          # VIVO (confirmado con dos pruebas de control en local: matando el PID de "$!" con
          # "npx ts-node", el servidor seguía respondiendo 5+ segundos después y quedaban
          # procesos node.exe vivos; con el binario de node_modules/.bin/ invocado DIRECTO, sin
          # npx, el mismo kill terminaba el proceso real en ~1s sin huérfanos). Ya arreglado en
          # la raíz en el paso anterior (binario directo, no "npx") — el `kill -0` de acá abajo
          # queda como defensa adicional por si el proceso tarda un poco en terminar de verdad,
          # no como el fix principal.
          if [ -f server.pid ]; then
            PID="$(cat server.pid)"
            kill "$PID" 2>/dev/null || true
            for i in $(seq 1 20); do
              if ! kill -0 "$PID" 2>/dev/null; then break; fi
              sleep 0.5
            done
            if kill -0 "$PID" 2>/dev/null; then
              echo "El servidor (PID $PID) no terminó a tiempo tras SIGTERM, forzando con SIGKILL."
              kill -9 "$PID" 2>/dev/null || true
              sleep 1
            fi
          fi
          # dev.sqlite3* ya no se genera (Postgres real via DATABASE_URL, ver "services" mas
          # arriba) - el "rm -f" se deja igual de inofensivo si algun dia reaparece un
          # archivo asi por error de configuracion, pero ya no es el mecanismo real de
          # limpieza entre Fase 1 y Fase 2 (ver la nota larga junto a "services: postgres"
          # sobre por que Fase 2 ya NO arranca con una base vacia).
          rm -f server.pid dev.sqlite3 dev.sqlite3-*

      - name: "Fase 2/2 - arrancar servidor limpio (ventana de cancelacion DEFAULT = 120 min)"
        env:
          PORT: "3000"
          ENABLE_DEV_ROUTES: "true"
          RATE_LIMIT_LOGIN_MAX: "1000"
          RATE_LIMIT_REGISTRO_MAX: "1000"
        run: |
          # Mismo motivo que en "Fase 1/2 - arrancar servidor": binario directo, no "npx".
          node_modules/.bin/ts-node --transpile-only src/index.ts > server-fase2.log 2>&1 &
          echo $! > server.pid
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3000/health > /dev/null; then
              echo "Servidor (fase 2) arriba."
              exit 0
            fi
            sleep 1
          done
          echo "El servidor (fase 2) no respondio /health a tiempo."
          cat server-fase2.log
          exit 1

      - name: "Fase 2/2 - correr test-rn8-ventana-cancelacion (necesita la ventana default)"
        run: |
          set +e
          node scripts/test-rn8-ventana-cancelacion.mjs > scripts-fase2-rn8.log 2>&1
          code=$?
          cat scripts-fase2-rn8.log
          exit $code

      - name: "Fase 2/2 - logs del servidor (si algo fallo)"
        if: failure()
        run: cat server-fase2.log || true

      - name: "Fase 2/2 - detener servidor"
        if: always()
        run: |
          # Mismo criterio que "Fase 1/2 - detener servidor y limpiar la DB" (ver el comentario
          # largo ahí): esperar a que el proceso realmente termine antes de seguir, en vez de
          # asumirlo tras el "kill". Acá no hay un tercer arranque en el mismo puerto dentro de
          # este job, pero se mantiene por consistencia y para no reintroducir el mismo patrón
          # fragil si en el futuro se agrega una Fase 3.
          if [ -f server.pid ]; then
            PID="$(cat server.pid)"
            kill "$PID" 2>/dev/null || true
            for i in $(seq 1 20); do
              if ! kill -0 "$PID" 2>/dev/null; then break; fi
              sleep 0.5
            done
            kill -9 "$PID" 2>/dev/null || true
          fi
          # dev.sqlite3* ya no se genera (Postgres real via DATABASE_URL, ver "services" mas
          # arriba) - el "rm -f" se deja igual de inofensivo si algun dia reaparece un
          # archivo asi por error de configuracion, pero ya no es el mecanismo real de
          # limpieza entre Fase 1 y Fase 2 (ver la nota larga junto a "services: postgres"
          # sobre por que Fase 2 ya NO arranca con una base vacia).
          rm -f server.pid dev.sqlite3 dev.sqlite3-*

      # Este entorno de desarrollo no tiene forma de leer logs de Actions: la API de descarga
      # de logs devuelve 403 "Must have admin rights to Repository" aun en un repo publico y
      # sin autenticacion (mismo bloqueo ya documentado en turnos-mobile-ci.yml, commit
      # cb20d12 - confirmado de nuevo puntualmente para este job al diagnosticar el run
      # 31108620102). Mismo workaround: publicamos los logs ya capturados en archivo por los
      # pasos de arriba como comentario en el commit que disparo el run, usando el
      # GITHUB_TOKEN que Actions provee automaticamente (permiso contents:write otorgado en
      # este job) - ese comentario si se puede leer despues sin login via la API publica (GET
      # /repos/{owner}/{repo}/commits/{sha}/comments).
      #
      # OJO (hallazgo de este ciclo, ver memory/proyectos/turnos-profesionales/decisiones.md):
      # la version anterior envolvia cada log en "<details><summary>...</summary>...
      # </details>". Eso SI queda en el body como texto plano (no como archivo adjunto - la
      # API de comentarios de commit no tiene ningun concepto de adjunto), pero <details> se
      # renderiza COLAPSADO por default: sin hacer click para expandirlo, lo unico que se ve
      # es el titulo del comentario y el nombre del archivo (el "resumen"), nunca el contenido
      # real del log. Se saca <details> por completo: el bloque de codigo va SIEMPRE visible,
      # sin ningun click ni sesion iniciada necesarios. Se agrega ademas truncado por lineas
      # (ultimas 200, con un tope extra en bytes por archivo) y un tope total del body: la
      # version anterior podia superar el limite de tamano de un comentario de GitHub si mas
      # de un archivo llegaba al maximo de 55000 bytes a la vez, lo que hubiera hecho fallar
      # el POST silenciosamente (sin romper el job, porque este paso no revisa el status code
      # de curl) - no era el problema de ESTE incidente puntual, pero es la misma clase de
      # "el mecanismo de diagnostico no muestra lo que necesito" que se esta corrigiendo aca.
      - name: "Publicar logs del job como comentario del commit (si algo fallo)"
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          {
            echo "### turnos-backend-ci.yml / build-and-test - run ${{ github.run_id }} (${{ github.event_name }}, commit ${{ github.sha }})"
            echo
            for f in roles-paso1.log roles-paso2.log roles-paso3.log roles-verificar-rls.log server-fase1.log scripts-fase1.log server-fase2.log scripts-fase2-rn8.log; do
              if [ -s "$f" ]; then
                echo "#### $f (ultimas 200 lineas)"
                echo
                echo '```'
                tail -n 200 "$f" | tail -c 20000
                echo '```'
                echo
              fi
            done
          } > /tmp/comment-body.md
          node -e "
            const fs = require('fs');
            let body = fs.readFileSync('/tmp/comment-body.md', 'utf8');
            const MAX = 60000;
            if (body.length > MAX) {
              body = '_(comentario truncado - se muestran los ultimos ' + MAX + ' caracteres)_\n\n' + body.slice(-MAX);
            }
            fs.writeFileSync('/tmp/comment-body.json', JSON.stringify({ body }));
          "
          curl -sS -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/commits/${{ github.sha }}/comments" \
            -d @/tmp/comment-body.json

  # ---------------------------------------------------------------------------------------
  # Job 2: valida el ARTEFACTO EMPAQUETADO (el Dockerfile de verdad, no el codigo suelto).
  # Este es el unico lugar donde este proyecto puede validar el Dockerfile con Docker real:
  # el entorno de desarrollo de DevOps no tiene Docker instalado (ver 08-despliegue/
  # README.md), pero los runners de GitHub Actions si lo traen preinstalado. Corre en
  # config "tipo produccion" (NODE_ENV=production del propio Dockerfile, sin
  # ENABLE_DEV_ROUTES) para que este smoke test sea representativo de un despliegue real -
  # incluida una conexion real a su propio Postgres efimero (--network host, ver el paso
  # "Levantar el contenedor" mas abajo), no un mock ni node:sqlite.
  # ---------------------------------------------------------------------------------------
  docker-build-smoke:
    name: Build de imagen Docker + smoke test end-to-end
    runs-on: ubuntu-latest
    needs: build-and-test
    # `contents: write` habilita el ultimo paso (comentario en el commit con los logs de una
    # falla) - mismo motivo/patron que el job build-and-test de este mismo workflow.
    permissions:
      contents: write
    # Service container de PostgreSQL (2026-08-07) - mismo motivo y mismas credenciales fijas
    # (no secretas) que el job build-and-test de mas arriba, ver el comentario largo ahi. Este
    # es un Postgres efimero PROPIO de este job (cada job de Actions corre en su propia
    # maquina nueva; no hay ningun dato compartido con el Postgres de build-and-test). La
    # diferencia real con ese otro job: aca la app no corre como step directo del runner sino
    # DENTRO de un contenedor levantado a mano con "docker run" (ver ese paso mas abajo), asi
    # que "localhost" adentro de ESE contenedor no ve por defecto el puerto publicado de este
    # service container - se resuelve con "--network host" en el "docker run", no con un
    # host/IP distinto en DATABASE_URL.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: turnos_ci
          POSTGRES_PASSWORD: turnos_ci_password
          POSTGRES_DB: turnos_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U turnos_ci -d turnos_test"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5
    # Nota operativa (visto en el run 31115744124): este job puede fallar directo en el paso
    # interno "Set up job" (lo genera GitHub, no un step de este archivo) con las anotaciones
    # "Failed to resolve action download info." / "Internal Server Error", sin atarse a ninguna
    # linea real de este YAML. Es un hiccup transitorio del servicio de Actions al resolver
    # `actions/checkout@v4`, no un defecto de este workflow - confirmado revisando las
    # annotations del check-run (GET /repos/.../check-runs/{id}/annotations, sin login) antes de
    # tocar nada por esa causa. Ante ese patron puntual, reintentar (nuevo push) en vez de
    # buscar un bug en el codigo.
    # Este job pisa el default de workflow (working-directory: backend/) por la raiz del
    # repo: `docker build <path>` necesita el path relativo a la raiz, no al backend. El
    # paso de smoke test de mas abajo vuelve a pisarlo puntualmente hacia backend/ porque
    # ese si necesita correr `node scripts/smoke-test.mjs` desde ahi.
    defaults:
      run:
        working-directory: .
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ── Separacion de roles de Postgres (2026-08-09/10, DevOps) ─────────────────────────────
      # Mismo patron y mismos 4 pasos que el job build-and-test de arriba (ver la nota grande
      # del encabezado de este archivo y los comentarios de esos pasos para el detalle completo
      # - no repetidos ahi por ahi). Unica diferencia real: working-directory por defecto de
      # este job es la raiz del repo ("." - ver "defaults" mas abajo), asi que las rutas a los
      # archivos de DBA son relativas a la raiz, no a backend/.
      - name: "Roles Postgres 0/4 - psql en el runner (ubuntu-latest ya lo trae; instalar si faltara)"
        run: |
          if ! command -v psql > /dev/null 2>&1; then
            sudo apt-get update && sudo apt-get install --yes postgresql-client
          fi
          psql --version

      - name: "Roles Postgres 1/4 - provisionar rol de MIGRACION y rol de RUNTIME"
        env:
          PGPASSWORD: turnos_ci_password
        run: |
          set +e
          {
            psql --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -v migrador_rol=turnos_ci_migrador -v migrador_password=turnos_ci_migrador_password \
              -v app_rol=turnos_ci_app -v app_password=turnos_ci_app_password \
              -v nombre_db=turnos_test \
              -f proyectos/turnos-profesionales/05-codigo/database/scripts/provisionar_roles_postgres.sql
            psql -v ON_ERROR_STOP=1 --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -c 'GRANT CREATE ON SCHEMA public TO turnos_ci_migrador;'
          } > roles-paso1.log 2>&1
          code=$?
          cat roles-paso1.log
          exit $code

      - name: "Roles Postgres 2/4 - migrar el esquema como el rol de MIGRACION"
        env:
          PGPASSWORD: turnos_ci_migrador_password
        run: |
          set +e
          {
            psql -v ON_ERROR_STOP=1 --host localhost --port 5432 --username turnos_ci_migrador --dbname turnos_test \
              -f proyectos/turnos-profesionales/05-codigo/database/migrations/001_init.sql
          } > roles-paso2.log 2>&1
          code=$?
          cat roles-paso2.log
          exit $code

      - name: "Roles Postgres 3/4 - completar GRANTs (funciones SECURITY DEFINER, ya existen)"
        env:
          PGPASSWORD: turnos_ci_password
        run: |
          set +e
          {
            psql --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -v migrador_rol=turnos_ci_migrador -v migrador_password=turnos_ci_migrador_password \
              -v app_rol=turnos_ci_app -v app_password=turnos_ci_app_password \
              -v nombre_db=turnos_test \
              -f proyectos/turnos-profesionales/05-codigo/database/scripts/provisionar_roles_postgres.sql
          } > roles-paso3.log 2>&1
          code=$?
          cat roles-paso3.log
          exit $code

      - name: "Roles Postgres 4/4 - verificar RLS a nivel de base de datos (verificar_rls_postgres.sql, Security)"
        env:
          PGPASSWORD: turnos_ci_password
        run: |
          set +e
          {
            psql -v ON_ERROR_STOP=1 --host localhost --port 5432 --username turnos_ci --dbname turnos_test \
              -v migrador_rol=turnos_ci_migrador -v app_rol=turnos_ci_app \
              -f proyectos/turnos-profesionales/05-codigo/database/scripts/verificar_rls_postgres.sql
          } > roles-verificar-rls.log 2>&1
          code=$?
          cat roles-verificar-rls.log
          exit $code

      # OJO (hallazgo de este ciclo): antes este paso no capturaba su propia salida a ningun
      # archivo. Cuando "docker build" fallaba de verdad (run 31117318338, commit 389fe65 -
      # ver memory/proyectos/turnos-profesionales/decisiones.md), el paso de diagnostico de
      # mas abajo no tenia NADA que publicar sobre la causa real: el unico archivo no vacio
      # terminaba siendo docker-container.log con el ruido de "No such container" (el
      # contenedor nunca llego a crearse). Se captura aca para que el comentario del commit
      # muestre el error real de "docker build", no solo ese efecto secundario.
      - name: Build de la imagen (Dockerfile multi-stage)
        run: |
          set +e
          docker build \
            -t turnos-profesionales-backend:ci \
            proyectos/turnos-profesionales/05-codigo/backend \
            > docker-build.log 2>&1
          code=$?
          cat docker-build.log
          exit $code

      - name: "Levantar el contenedor (config tipo produccion - sin ENABLE_DEV_ROUTES)"
        run: |
          set +e
          # --network host (en vez de "-p 3000:3000"): el service container de postgres de
          # arriba publica su puerto 5432 en el localhost DEL RUNNER, no en una red docker
          # que este contenedor comparta automaticamente - un "docker run" suelto (no
          # administrado por Actions, a diferencia de un step normal) no ve ese localhost
          # salvo que comparta la red del host explicitamente. Con --network host, tanto
          # "localhost:5432" (Postgres) como el puerto 3000 de la app quedan expuestos
          # directo en el runner sin mapeo aparte - "-p" se saca del todo (Docker lo ignora
          # con un warning bajo --network host; mejor no dejarlo de adorno).
          # DATABASE_URL apunta al rol de RUNTIME (turnos_ci_app, ya provisionado por los pasos
          # "Roles Postgres 1/4-3/4" de arriba) - NUNCA al superusuario de bootstrap turnos_ci
          # ni al rol de MIGRACION, mismo criterio que docker-compose.yml y el job
          # build-and-test de este mismo archivo (ver la nota grande del encabezado).
          docker run -d \
            --name turnos-backend-ci \
            --network host \
            -e JWT_SECRET="ci-smoke-test-secret-nunca-usar-en-produccion" \
            -e DATABASE_URL="postgresql://turnos_ci_app:turnos_ci_app_password@localhost:5432/turnos_test" \
            turnos-profesionales-backend:ci \
            > docker-run.log 2>&1
          code=$?
          cat docker-run.log
          exit $code

      - name: Esperar a que el HEALTHCHECK / GET /health responda
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://localhost:3000/health > /dev/null; then
              echo "Contenedor arriba."
              exit 0
            fi
            sleep 1
          done
          echo "El contenedor no respondio /health a tiempo."
          docker logs turnos-backend-ci > docker-container.log 2>&1 || true
          cat docker-container.log || true
          exit 1

      - name: Smoke test end-to-end contra el contenedor real
        working-directory: proyectos/turnos-profesionales/05-codigo/backend
        run: |
          set +e
          node scripts/smoke-test.mjs > smoke-test.log 2>&1
          code=$?
          cat smoke-test.log
          exit $code

      - name: Logs del contenedor (si algo fallo)
        if: failure()
        run: |
          docker logs turnos-backend-ci > docker-container.log 2>&1 || true
          cat docker-container.log || true

      - name: Apagar y borrar el contenedor
        if: always()
        run: docker rm -f turnos-backend-ci || true

      # Mismo bloqueo de logs de Actions sin login que en el job build-and-test de este mismo
      # workflow (y en turnos-mobile-ci.yml, commit cb20d12) - mismo workaround, y misma
      # correccion de este ciclo (ver el comentario largo en el paso equivalente del job
      # build-and-test, mas arriba, para el detalle completo de por que se saca <details>).
      # Se agregan ademas docker-build.log/docker-run.log a la lista de archivos: antes solo
      # se publicaban docker-container.log y smoke-test.log, asi que si "docker build" o
      # "docker run" fallaban (en vez del healthcheck o el smoke test), el comentario
      # quedaba vacio de informacion util - exactamente lo que paso en el run 31117318338
      # (commit 389fe65), ver memory/proyectos/turnos-profesionales/decisiones.md.
      - name: "Publicar logs del job como comentario del commit (si algo fallo)"
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          {
            echo "### turnos-backend-ci.yml / docker-build-smoke - run ${{ github.run_id }} (${{ github.event_name }}, commit ${{ github.sha }})"
            echo
            for f in roles-paso1.log roles-paso2.log roles-paso3.log roles-verificar-rls.log docker-build.log docker-run.log docker-container.log proyectos/turnos-profesionales/05-codigo/backend/smoke-test.log; do
              if [ -s "$f" ]; then
                echo "#### $f (ultimas 200 lineas)"
                echo
                echo '```'
                tail -n 200 "$f" | tail -c 20000
                echo '```'
                echo
              fi
            done
          } > /tmp/comment-body.md
          node -e "
            const fs = require('fs');
            let body = fs.readFileSync('/tmp/comment-body.md', 'utf8');
            const MAX = 60000;
            if (body.length > MAX) {
              body = '_(comentario truncado - se muestran los ultimos ' + MAX + ' caracteres)_\n\n' + body.slice(-MAX);
            }
            fs.writeFileSync('/tmp/comment-body.json', JSON.stringify({ body }));
          "
          curl -sS -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/commits/${{ github.sha }}/comments" \
            -d @/tmp/comment-body.json
